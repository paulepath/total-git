<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="using:TotalGit.App.ViewModels"
        xmlns:views="using:TotalGit.App.Views"
        x:Class="TotalGit.App.Views.MainWindow"
        x:DataType="vm:MainWindowViewModel"
        Title="{Binding RepositoryName, StringFormat='TotalGit - {0}'}"
        Width="1500" Height="900"
        Background="{StaticResource WindowBackgroundBrush}">
  <Window.KeyBindings>
    <KeyBinding Gesture="F5" Command="{Binding RefreshCommand}" />
    <KeyBinding Gesture="Ctrl+O" Command="{Binding OpenRepositoryCommand}" />
    <KeyBinding Gesture="Escape" Command="{Binding CloseDiffCommand}" />
  </Window.KeyBindings>

  <Window.Resources>
    <StreamGeometry x:Key="InlineDiffIcon">M3,4H21V6H3V4M3,9H21V11H3V9M3,14H21V16H3V14M3,19H21V21H3V19Z</StreamGeometry>
    <StreamGeometry x:Key="SplitDiffIcon">M3,3H11V21H3V3M5,5V19H9V5H5M13,3H21V21H13V3M15,5V19H19V5H15Z</StreamGeometry>
    <StreamGeometry x:Key="FolderIcon">M10,4H4C2.89,4 2,4.89 2,6V18A2,2 0 0,0 4,20H20A2,2 0 0,0 22,18V8C22,6.89 21.1,6 20,6H12L10,4Z</StreamGeometry>
    <StreamGeometry x:Key="ChevronIcon">M8.59,16.58L13.17,12L8.59,7.41L10,6L16,12L10,18L8.59,16.58Z</StreamGeometry>
    <StreamGeometry x:Key="FetchIcon">M5,20H19V18H5M19,9H15V3H9V9H5L12,16L19,9Z</StreamGeometry>
    <StreamGeometry x:Key="PullIcon">M9,4H15V12H19.84L12,19.84L4.16,12H9V4Z</StreamGeometry>
    <StreamGeometry x:Key="PushIcon">M15,20H9V12H4.16L12,4.16L19.84,12H15V20Z</StreamGeometry>
    <StreamGeometry x:Key="WorktreeIcon">M3,3H9V7H3V3M15,10H21V14H15V10M15,17H21V21H15V17M13,13H7V18H13V20H7L5,20V9H7V11H13V13Z</StreamGeometry>
    <StreamGeometry x:Key="CodeIcon">M17,16.47V7.39L11,11.93M2.22,9.19C1.92,8.87 1.91,8.37 2.2,8.04L3.4,6.93C3.6,6.75 4.09,6.67 4.45,6.93L7.87,9.54L15.8,2.29C16.12,1.97 16.67,1.84 17.3,2.17L21.3,4.08C21.66,4.29 22,4.62 22,5.23V18.72C22,19.12 21.71,19.55 21.4,19.72L17,21.83C16.68,21.96 16.08,21.84 15.87,21.63L7.85,14.33L4.45,16.93C4.07,17.19 3.6,17.12 3.4,16.93L2.2,15.83C1.88,15.5 1.92,14.96 2.25,14.63L5.25,11.93</StreamGeometry>
    <StreamGeometry x:Key="RefreshIcon">M17.65,6.35C16.2,4.9 14.21,4 12,4A8,8 0 0,0 4,12A8,8 0 0,0 12,20C15.73,20 18.84,17.45 19.73,14H17.65C16.83,16.33 14.61,18 12,18A6,6 0 0,1 6,12A6,6 0 0,1 12,6C13.66,6 15.14,6.69 16.22,7.78L13,11H20V4L17.65,6.35Z</StreamGeometry>
    <StreamGeometry x:Key="CloseIcon">M19,6.41L17.59,5L12,10.59L6.41,5L5,6.41L10.59,12L5,17.59L6.41,19L12,13.41L17.59,19L19,17.59L13.41,12L19,6.41Z</StreamGeometry>
  </Window.Resources>

  <Window.Styles>
    <Style Selector="Button.tool">
      <Setter Property="Background" Value="Transparent" />
      <Setter Property="Padding" Value="10,4" />
      <Setter Property="MinWidth" Value="56" />
      <Setter Property="VerticalAlignment" Value="Center" />
    </Style>
    <Style Selector="Button.tool PathIcon">
      <Setter Property="Width" Value="18" />
      <Setter Property="Height" Value="18" />
      <Setter Property="HorizontalAlignment" Value="Center" />
    </Style>
    <Style Selector="Button.tool TextBlock.label">
      <Setter Property="FontSize" Value="11" />
      <Setter Property="HorizontalAlignment" Value="Center" />
      <Setter Property="Margin" Value="0,3,0,0" />
    </Style>
    <Style Selector="TextBlock.count">
      <Setter Property="FontSize" Value="10" />
      <Setter Property="Foreground" Value="#5AA9F2" />
      <Setter Property="Margin" Value="2,0,0,0" />
    </Style>
    <Style Selector="Button.seg">
      <Setter Property="Background" Value="#2A2F36" />
      <Setter Property="Foreground" Value="#A9AFB8" />
    </Style>
    <Style Selector="Button.seg.active">
      <Setter Property="Background" Value="#2D5E9E" />
      <Setter Property="Foreground" Value="White" />
    </Style>
    <Style Selector="Button.seg.active:pointerover /template/ ContentPresenter#PART_ContentPresenter">
      <Setter Property="Background" Value="#3570B8" />
      <Setter Property="Foreground" Value="White" />
    </Style>
    <Style Selector="GridSplitter">
      <Setter Property="Background" Value="#2B2F36" />
    </Style>
  </Window.Styles>

  <DockPanel>
    <!-- Toolbar -->
    <Border DockPanel.Dock="Top" Background="{StaticResource ToolbarBackgroundBrush}" Padding="10,6">
      <Grid ColumnDefinitions="Auto,Auto,*,Auto,Auto">
        <Button Grid.Column="0" Classes="tool" Command="{Binding OpenRepositoryCommand}" ToolTip.Tip="Open repository (Ctrl+O)">
          <StackPanel>
            <PathIcon Data="{StaticResource FolderIcon}" />
            <TextBlock Classes="label" Text="Open" />
          </StackPanel>
        </Button>

        <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="14,0,0,0" VerticalAlignment="Center">
          <StackPanel>
            <TextBlock Text="repository" FontSize="11" Foreground="{StaticResource MutedTextBrush}" />
            <TextBlock Text="{Binding RepositoryName}" FontSize="16" FontWeight="SemiBold" Foreground="{StaticResource PrimaryTextBrush}" />
          </StackPanel>
          <PathIcon Width="10" Height="10" Margin="14,14,14,0" Foreground="{StaticResource MutedTextBrush}" Data="{StaticResource ChevronIcon}"
                    IsVisible="{Binding HasWorktreeName}" />
          <StackPanel IsVisible="{Binding HasWorktreeName}">
            <TextBlock Text="worktree" FontSize="11" Foreground="{StaticResource MutedTextBrush}" />
            <TextBlock Text="{Binding WorktreeName}" FontSize="16" FontWeight="SemiBold" Foreground="#B084F5" />
          </StackPanel>
          <PathIcon Width="10" Height="10" Margin="14,14,14,0" Foreground="{StaticResource MutedTextBrush}" Data="{StaticResource ChevronIcon}" />
          <StackPanel>
            <TextBlock Text="branch" FontSize="11" Foreground="{StaticResource MutedTextBrush}" />
            <TextBlock Text="{Binding CurrentBranch}" FontSize="16" FontWeight="SemiBold" Foreground="{StaticResource PrimaryTextBrush}" />
          </StackPanel>
        </StackPanel>

        <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Center" Spacing="2" IsEnabled="{Binding HasRepository}">
          <Button Classes="tool" Command="{Binding FetchCommand}" ToolTip.Tip="Fetch all remotes">
            <StackPanel>
              <PathIcon Data="{StaticResource FetchIcon}" />
              <TextBlock Classes="label" Text="Fetch" />
            </StackPanel>
          </Button>
          <Button Classes="tool" Command="{Binding PullCommand}" ToolTip.Tip="Pull the current branch">
            <StackPanel>
              <PathIcon Data="{StaticResource PullIcon}" />
              <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <TextBlock Classes="label" Text="Pull" />
                <TextBlock Classes="count" Text="{Binding BehindText}" Margin="3,3,0,0" />
              </StackPanel>
            </StackPanel>
          </Button>
          <Button Classes="tool" Command="{Binding PushCommand}" ToolTip.Tip="Push the current branch">
            <StackPanel>
              <PathIcon Data="{StaticResource PushIcon}" />
              <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <TextBlock Classes="label" Text="Push" />
                <TextBlock Classes="count" Text="{Binding AheadText}" Margin="3,3,0,0" />
              </StackPanel>
            </StackPanel>
          </Button>
          <Border Width="1" Background="#3A3F47" Margin="8,6" />
          <Button Classes="tool" Command="{Binding CreateWorktreeCommand}" ToolTip.Tip="Create a worktree with a new branch from HEAD">
            <StackPanel>
              <PathIcon Data="{StaticResource WorktreeIcon}" />
              <TextBlock Classes="label" Text="Worktree" />
            </StackPanel>
          </Button>
          <Button Classes="tool" Command="{Binding OpenInVsCodeCommand}" ToolTip.Tip="Open this worktree in VS Code">
            <StackPanel>
              <PathIcon Data="{StaticResource CodeIcon}" Foreground="#3FA0F0" />
              <TextBlock Classes="label" Text="VS Code" />
            </StackPanel>
          </Button>
        </StackPanel>

        <StackPanel Grid.Column="3" Orientation="Horizontal" VerticalAlignment="Center" Spacing="8" Margin="0,0,10,0">
          <ProgressBar IsIndeterminate="True" Width="60" IsVisible="{Binding IsBusy}" />
          <TextBlock Text="{Binding BusyText}" Foreground="{StaticResource MutedTextBrush}" IsVisible="{Binding IsBusy}" />
          <TextBlock Text="{Binding CommitCountText}" Foreground="{StaticResource MutedTextBrush}" IsVisible="{Binding !IsBusy}" />
        </StackPanel>

        <Button Grid.Column="4" Classes="tool" Command="{Binding RefreshCommand}" IsEnabled="{Binding HasRepository}" ToolTip.Tip="Refresh (F5)">
          <StackPanel>
            <PathIcon Data="{StaticResource RefreshIcon}" />
            <TextBlock Classes="label" Text="Refresh" />
          </StackPanel>
        </Button>
      </Grid>
    </Border>

    <!-- Banner -->
    <Border DockPanel.Dock="Top" IsVisible="{Binding Banner, Converter={x:Static ObjectConverters.IsNotNull}}"
            Background="{Binding Banner.IsError, Converter={x:Static vm:Converters.ErrorToBackground}, FallbackValue=Transparent}"
            Padding="14,8">
      <Grid ColumnDefinitions="*,Auto,Auto">
        <SelectableTextBlock Text="{Binding Banner.Message}" TextWrapping="Wrap" VerticalAlignment="Center" Foreground="#F0F2F5" />
        <ItemsControl Grid.Column="1" ItemsSource="{Binding Banner.Actions}" Margin="12,0,0,0">
          <ItemsControl.ItemsPanel>
            <ItemsPanelTemplate>
              <StackPanel Orientation="Horizontal" Spacing="6" />
            </ItemsPanelTemplate>
          </ItemsControl.ItemsPanel>
          <ItemsControl.ItemTemplate>
            <DataTemplate x:DataType="vm:MenuAction">
              <Button Content="{Binding Header}" Command="{Binding Command}" CommandParameter="{Binding Parameter}" Padding="10,3" />
            </DataTemplate>
          </ItemsControl.ItemTemplate>
        </ItemsControl>
        <Button Grid.Column="2" Command="{Binding DismissBannerCommand}" Background="Transparent" Padding="6" Margin="8,0,0,0" ToolTip.Tip="Dismiss">
          <PathIcon Data="{StaticResource CloseIcon}" Width="12" Height="12" />
        </Button>
      </Grid>
    </Border>

    <Grid x:Name="MainGrid" ColumnDefinitions="240,4,*,4,380">
      <views:SidebarView Grid.Column="0" x:Name="Sidebar" DataContext="{Binding Sidebar}" />
      <GridSplitter Grid.Column="1" ResizeDirection="Columns" />

      <Panel Grid.Column="2">
        <Grid ColumnDefinitions="*,Auto" IsVisible="{Binding HasRepository}">
          <views:CommitGraphView Grid.Column="0" x:Name="Graph" Data="{Binding Graph}" SelectedSha="{Binding SelectedSha}" />
          <ScrollBar Grid.Column="1" x:Name="GraphScrollBar" Orientation="Vertical" AllowAutoHide="False" />
        </Grid>

        <!-- Diff replaces the graph while a file is open, like GitKraken. -->
        <DockPanel IsVisible="{Binding HasDiff}" Background="#1C1F24">
          <Border DockPanel.Dock="Top" Background="#23272D" Padding="12,6" BorderBrush="#30353C" BorderThickness="0,0,0,1">
            <Grid ColumnDefinitions="*,Auto,Auto">
              <TextBlock Text="{Binding DiffTitle}" VerticalAlignment="Center" FontFamily="Cascadia Mono, Consolas, monospace" FontSize="12"
                         Foreground="#E6E8EB" TextTrimming="CharacterEllipsis" />
              <!-- Inline | Side by side -->
              <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="12,0,10,0">
                <Button Classes="seg" Classes.active="{Binding IsInlineDiff}" Command="{Binding SetDiffModeCommand}"
                              CommandParameter="{x:Static vm:DiffViewMode.Inline}" Padding="10,3" CornerRadius="4,0,0,4"
                              ToolTip.Tip="Inline diff">
                  <StackPanel Orientation="Horizontal" Spacing="6">
                    <PathIcon Data="{StaticResource InlineDiffIcon}" Width="12" Height="12" />
                    <TextBlock Text="Inline" FontSize="12" />
                  </StackPanel>
                </Button>
                <Button Classes="seg" Classes.active="{Binding IsSplitDiff}" Command="{Binding SetDiffModeCommand}"
                              CommandParameter="{x:Static vm:DiffViewMode.Split}" Padding="10,3" CornerRadius="0,4,4,0"
                              ToolTip.Tip="Side-by-side diff">
                  <StackPanel Orientation="Horizontal" Spacing="6">
                    <PathIcon Data="{StaticResource SplitDiffIcon}" Width="12" Height="12" />
                    <TextBlock Text="Side by side" FontSize="12" />
                  </StackPanel>
                </Button>
              </StackPanel>
              <Button Grid.Column="2" Command="{Binding CloseDiffCommand}" Padding="10,3" ToolTip.Tip="Close (Esc)">
                <StackPanel Orientation="Horizontal" Spacing="6">
                  <PathIcon Data="{StaticResource CloseIcon}" Width="10" Height="10" />
                  <TextBlock Text="Close" FontSize="12" />
                </StackPanel>
              </Button>
            </Grid>
          </Border>
          <Grid ColumnDefinitions="*,Auto">
            <views:DiffView x:Name="DiffView" Diff="{Binding Diff}" Mode="{Binding DiffMode}" />
            <ScrollBar Grid.Column="1" x:Name="DiffScrollBar" Orientation="Vertical" AllowAutoHide="False" />
          </Grid>
        </DockPanel>

        <StackPanel IsVisible="{Binding ShowEmptyState}" HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="12">
          <TextBlock Text="Open a git repository to view its history" FontSize="16"
                     Foreground="{StaticResource MutedTextBrush}" HorizontalAlignment="Center" />
          <Button Content="Open repository..." Command="{Binding OpenRepositoryCommand}" HorizontalAlignment="Center" />
        </StackPanel>

        <TextBlock IsVisible="{Binding IsLoading}" Text="Loading repository..." FontSize="16"
                   Foreground="{StaticResource MutedTextBrush}" HorizontalAlignment="Center" VerticalAlignment="Center" />

        <StackPanel IsVisible="{Binding LoadError, Converter={x:Static ObjectConverters.IsNotNull}}"
                    HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="12" MaxWidth="600">
          <TextBlock Text="{Binding LoadError}" Foreground="#F26B6B" TextWrapping="Wrap" HorizontalAlignment="Center" />
          <Button Content="Open a different repository..." Command="{Binding OpenRepositoryCommand}" HorizontalAlignment="Center" />
        </StackPanel>
      </Panel>

      <GridSplitter Grid.Column="3" ResizeDirection="Columns" />

      <Panel Grid.Column="4" Background="#23272D">
        <views:CommitDetailsView x:Name="DetailsView" DataContext="{Binding Details}" IsVisible="{Binding $parent[Window].((vm:MainWindowViewModel)DataContext).ShowDetails}" />
        <views:StagingView DataContext="{Binding Staging}" IsVisible="{Binding $parent[Window].((vm:MainWindowViewModel)DataContext).ShowStaging}" />
        <TextBlock Text="Select a commit to see its details" Foreground="{StaticResource MutedTextBrush}"
                   HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="12">
          <TextBlock.IsVisible>
            <MultiBinding Converter="{x:Static BoolConverters.And}">
              <Binding Path="!ShowDetails" />
              <Binding Path="!ShowStaging" />
              <Binding Path="HasRepository" />
            </MultiBinding>
          </TextBlock.IsVisible>
        </TextBlock>
      </Panel>
    </Grid>
  </DockPanel>
</Window>
